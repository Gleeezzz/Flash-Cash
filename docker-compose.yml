version: '3.8'
services:
  mysql-db:
    image: mysql:8.0
    container_name: flashcash-db
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: flashcash
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data: